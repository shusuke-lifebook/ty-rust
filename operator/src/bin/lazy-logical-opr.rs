fn main() {
    // 遅延論理演算
    let a = 10;
    println!("a >= 0 && a < 10: {}", a >= 0 && a <= 10);
    println!("a < 0 || a > 10: {}", a < 0 || a > 10);
    //println!("a > 100 ^ a < -100: {}", a > 100 ^^ a < -100);
    //println!("!(a == 0): {}", !(a == 0));
}
